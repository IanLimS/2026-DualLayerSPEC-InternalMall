# Internal Employee Benefits Mall - Protocols and Data

|- **Updated Date**: 2026-02-26
|- **Status**: In Development

## Unified Response Format

```json
{
  "success": boolean,
  "message": "string",
  "data": object | array,
  "error": { "code": "string", "message": "string", "details": "string" },
  "timestamp": "ISO8601"
}
```

## Error Codes

| Error Codes | HTTPStatusma | miao shu |
|--------|-----------|------|
| AUTH_001 | 401 | Unauthorizedfang wen |
| AUTH_002 | 401 | TokenyiExpired |
| AUTH_003 | 401 | TokenInvalid |
| AUTH_004 | 403 | Insufficient permissions |
| USER_001 | 400 | UsernamehuoPasswordError |
| USER_002 | 404 | User not found |
| USER_003 | 409 | Usernameyi cun zai |
| PROD_001 | 404 | Product not found |
| PROD_002 | 400 | ProductInsufficient stock |
| CART_001 | 400 | Cart is empty |
| ORDER_001 | 400 | Failed to create order |
| ORDER_002 | 404 | Order not found |
| SYS_001 | 500 | Systemnei buError |
| SYS_002 | 503 | fu wu zan bu ke yong |

## APIduan dian qing dan

### Health Check `/api/health`

| fang fa | lu jing | shuo ming | Backendshi xian |
|------|------|------|----------|
| GET | `/api/health` | fu wuHealth Check | ✅ |
| GET | `/api/health/database` | DatabaseStatusjian cha | ✅ |

### ren zheng `/api/auth`

| fang fa | lu jing | shuo ming | Backendshi xian |
|------|------|------|----------|
| POST | `/api/auth/login` | UserLogin | ✅ |
| POST | `/api/auth/logout` | UserLogout | ✅ |

### User `/api/user`

| fang fa | lu jing | shuo ming | Backendshi xian |
|------|------|------|----------|
| GET | `/api/user/profile` | huo quUserInfo | ✅ |
| PUT | `/api/user/profile` | UpdateUserInfo | ❌ Frontendyi ding yi jie kou danBackendNot implemented |
| GET | `/api/user/test-products` | TestProductList(mock) | ✅ |

### Admin `/api/admin`

| fang fa | lu jing | shuo ming | Backendshi xian |
|------|------|------|----------|
| POST | `/api/admin/login` | AdminLogin | ✅ |
| GET | `/api/admin/system-status` | SystemStatus | ✅ |
| GET | `/api/admin/test` | ConnectivityTest | ✅ |
| GET | `/api/admin/products` | ProductList(Pagination/Filter) | ✅ |
| POST | `/api/admin/products` | CreateProduct | ✅ |
| PUT | `/api/admin/products/:id` | UpdateProduct | ✅ |
| DELETE | `/api/admin/products/:id` | DeleteProduct | ✅ |
| PUT | `/api/admin/products/:id/status` | ProductshangUnpublish | ✅ |
| PUT | `/api/admin/products/:id/stock` | Stocktiao zheng | ✅ |
| PUT | `/api/admin/products/batch-status` | BatchUpdateStatus | ✅ |
| DELETE | `/api/admin/products/batch` | BatchDelete | ✅ |
| POST | `/api/admin/products/upload-image` | dan tuUpload | ✅ |
| POST | `/api/admin/products/upload-images` | duo tuUpload | ✅ |
| GET | `/api/admin/products/low-stock` | Low stock warning | ✅ |
| GET | `/api/admin/products/top-selling` | Top sellingpai hang | ✅ |
| GET | `/api/admin/categories` | CategoryList | ✅ |
| POST | `/api/admin/categories` | CreateCategory | ✅ |
| PUT | `/api/admin/categories/:id` | UpdateCategory | ✅ |
| DELETE | `/api/admin/categories/:id` | DeleteCategory | ✅ |
| GET | `/api/admin/products/:id` | Product Detail | ❌ Frontendyi ding yi danBackendNot implemented |
| POST | `/api/admin/products/import` | BatchImport | ❌ Frontendyi ding yi danBackendNot implemented |
| GET | `/api/admin/products/export` | ExportProduct | ❌ Frontendyi ding yi danBackendNot implemented |
| GET | `/api/admin/orders` | OrderList | ❌ Frontendyi ding yi danBackendNot implemented |
| GET | `/api/admin/orders/:id` | OrderDetails | ❌ Frontendyi ding yi danBackendNot implemented |
| PUT | `/api/admin/orders/:id/status` | OrderStatusUpdate | ❌ Frontendyi ding yi danBackendNot implemented |
| GET | `/api/admin/users` | UserList | ❌ Frontendyi ding yi danBackendNot implemented |
| GET | `/api/admin/users/:id` | UserDetails | ❌ Frontendyi ding yi danBackendNot implemented |
| PUT | `/api/admin/users/:id/points` | tiao zhengPoints | ❌ Frontendyi ding yi danBackendNot implemented |
| PUT | `/api/admin/users/:id/status` | UserStatusUpdate | ❌ Frontendyi ding yi danBackendNot implemented |
| GET | `/api/admin/settings` | huo quSystem Settings | ❌ Frontendyi ding yi danBackendNot implemented |
| PUT | `/api/admin/settings` | UpdateSystem Settings | ❌ Frontendyi ding yi danBackendNot implemented |
| GET | `/api/admin/statistics/sales` | xiao shouStatistics | ❌ Frontendyi ding yi danBackendNot implemented |
| GET | `/api/admin/statistics/users` | UserStatistics | ❌ Frontendyi ding yi danBackendNot implemented |
| GET | `/api/admin/statistics/products` | ProductStatistics | ❌ Frontendyi ding yi danBackendNot implemented |

### Product(gong kai) `/api/products`

| fang fa | lu jing | shuo ming | Backendshi xian |
|------|------|------|----------|
| GET | `/api/products` | ProductList | ✅ |
| GET | `/api/products/search` | ProductSearch | ✅ |
| GET | `/api/products/categories` | CategoryList | ✅ |
| GET | `/api/products/favorites` | UserFavoritesList | ✅ |
| GET | `/api/products/:id` | Product Detail | ✅ |
| POST | `/api/products/:id/favorite` | tian jiaFavorites | ✅ |
| DELETE | `/api/products/:id/favorite` | qu xiaoFavorites | ✅ |

### Cart `/api/cart`

| fang fa | lu jing | shuo ming | Backendshi xian |
|------|------|------|----------|
| GET | `/api/cart` | CartList | ✅ |
| POST | `/api/cart` | tian jiaProduct | ✅ |
| GET | `/api/cart/summary` | StatisticsInfo | ✅ |
| GET | `/api/cart/selected` | xuan zhongProduct | ✅ |
| PUT | `/api/cart/batch-update-selection` | BatchUpdatexuan zhong | ✅ |
| DELETE | `/api/cart/batch-remove` | BatchDelete | ✅ |
| DELETE | `/api/cart/clear` | qing kongCart | ✅ |
| PUT | `/api/cart/:cartId` | Updateshu liang | ✅ |
| PUT | `/api/cart/:cartId/selection` | Updatexuan zhongStatus | ✅ |
| DELETE | `/api/cart/:cartId` | DeleteProduct | ✅ |

## Database Design

### Design Principles

1. SQLiteDatabase，biao ming fu shu xing shi，zi duan ming xia hua xian ming ming fa
2. zhu jian zi zeng zheng shuid（ordersbiao chu wai，shi yongTEXTzhu jian）
3. shi jian zi duan: created_at / updated_at / deleted_at
4. Statuszi duan shi yong mei ju zhi zi fu chuan
5. jin e/Pointsshi yong zheng shu lei xing

### Table Structure Overview

gong **11zhang biao**，init.js zhongCreate：

#### users (Userbiao)
```sql
CREATE TABLE users (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  username TEXT UNIQUE NOT NULL,
  password TEXT NOT NULL,
  email TEXT,
  phone TEXT,
  points INTEGER DEFAULT 0,
  role TEXT DEFAULT 'user',          -- 'user' | 'admin'
  department TEXT,
  position TEXT,
  avatar TEXT,
  status TEXT DEFAULT 'active',      -- 'active' | 'inactive'
  last_login_at DATETIME,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  deleted_at DATETIME
);
```

#### categories (ProductCategorybiao)
```sql
CREATE TABLE categories (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  description TEXT,
  sort INTEGER DEFAULT 0,
  icon TEXT,
  status TEXT DEFAULT 'active',      -- 'active' | 'inactive'
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  deleted_at DATETIME
);
```

#### products (Productbiao)
```sql
CREATE TABLE products (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  name TEXT NOT NULL,
  description TEXT,
  specifications TEXT,               -- JSONge shi gui ge can shu
  price REAL NOT NULL,
  points_required INTEGER NOT NULL,
  stock INTEGER DEFAULT 0,
  warning_stock INTEGER DEFAULT 10,
  images TEXT,                       -- JSONge shi tu pianURLshu zu
  status TEXT DEFAULT 'active',      -- 'active' | 'inactive'
  category_id INTEGER,
  sales INTEGER DEFAULT 0,
  views INTEGER DEFAULT 0,
  favorites INTEGER DEFAULT 0,
  exchange_rules TEXT,
  sort INTEGER DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  deleted_at DATETIME,
  FOREIGN KEY (category_id) REFERENCES categories (id)
);
```

#### addresses (Shipping Addressbiao)
```sql
CREATE TABLE addresses (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL,
  recipient_name TEXT NOT NULL,
  phone TEXT NOT NULL,
  province TEXT NOT NULL,
  city TEXT NOT NULL,
  district TEXT NOT NULL,
  address TEXT NOT NULL,
  is_default INTEGER DEFAULT 0,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  deleted_at DATETIME,
  FOREIGN KEY (user_id) REFERENCES users (id)
);
```

#### cart (Cartbiao)
```sql
CREATE TABLE cart (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL,
  product_id INTEGER NOT NULL,
  quantity INTEGER NOT NULL DEFAULT 1,
  is_selected INTEGER DEFAULT 1,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users (id),
  FOREIGN KEY (product_id) REFERENCES products (id),
  UNIQUE(user_id, product_id)
);
```

#### orders (Orderbiao)
```sql
CREATE TABLE orders (
  id TEXT PRIMARY KEY,               -- wen ben zhu jian(Orderhao)
  user_id INTEGER NOT NULL,
  total_points INTEGER NOT NULL,
  status TEXT DEFAULT 'pending',     -- 'pending' | 'processing' | 'shipped' | 'completed' | 'cancelled'
  shipping_address TEXT NOT NULL,
  remark TEXT,
  cancelled_at DATETIME,
  completed_at DATETIME,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  deleted_at DATETIME,
  FOREIGN KEY (user_id) REFERENCES users (id)
);
```

#### order_items (Orderming xi biao)
```sql
CREATE TABLE order_items (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  order_id TEXT NOT NULL,
  product_id INTEGER NOT NULL,
  product_name TEXT NOT NULL,
  product_image TEXT,
  quantity INTEGER NOT NULL,
  points_required INTEGER NOT NULL,
  total_points INTEGER NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (order_id) REFERENCES orders (id),
  FOREIGN KEY (product_id) REFERENCES products (id)
);
```

#### points_history (Pointsji lu biao)
```sql
CREATE TABLE points_history (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL,
  points_change INTEGER NOT NULL,
  type TEXT NOT NULL,                -- bian dong lei xing
  description TEXT,
  reference_id TEXT,
  reference_type TEXT,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users (id)
);
```

#### favorites (ProductFavoritesbiao)
```sql
CREATE TABLE favorites (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL,
  product_id INTEGER NOT NULL,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users (id),
  FOREIGN KEY (product_id) REFERENCES products (id),
  UNIQUE(user_id, product_id)
);
```

#### system_configs (Systempei zhi biao)
```sql
CREATE TABLE system_configs (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  key TEXT UNIQUE NOT NULL,
  value TEXT,
  description TEXT,
  type TEXT DEFAULT 'string',
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### checkins (Userqian dao biao)
```sql
CREATE TABLE checkins (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  user_id INTEGER NOT NULL,
  checkin_date DATE NOT NULL,
  points_earned INTEGER NOT NULL DEFAULT 10,
  created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users (id),
  UNIQUE(user_id, checkin_date)
);
```

### Seed Data

- 4geUser: user1(100Points) / user2(200Points) / user3(150Points) / admin
- 5geCategory: Electronics / Daily Essentials / Office Supplies / Sports & Health / Books & Media
- 6geProduct: Smartphone / Bluetooth Earbuds / Insulated Bottle / Notebook / Yoga Mat / Programming Book
- 7tiaoSystempei zhi

### Image Storage

- Uploadlu jing: `/uploads/products/`
- wen jian ming ming: `compressed_${uuid}.jpg`
- URLge shi: `/uploads/products/compressed_${uuid}.jpg`
- ya suo can shu: Sharp, kuan800px, gao600px, zhi liang80%
- Databasecun chu: JSONge shiURLshu zu
